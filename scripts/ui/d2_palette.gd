class_name D2Palette

## Paleta de colores estilo Diablo II (minimalista, sin assets externos).
## Centraliza todos los tonos para mantener coherencia visual entre escenas.

# Piedra (fondos y paneles sunken)
const STONE_DARK := Color(0.10, 0.08, 0.06)
const STONE_MID := Color(0.18, 0.15, 0.12)
const STONE_LIGHT := Color(0.28, 0.24, 0.18)
const STONE_HIGHLIGHT := Color(0.38, 0.33, 0.24)

# Bronce (bordes y marcos)
const BRONZE_DARK := Color(0.32, 0.22, 0.10)
const BRONZE_MID := Color(0.50, 0.36, 0.18)
const BRONZE_LIGHT := Color(0.65, 0.50, 0.28)
const BRONZE_HI := Color(0.85, 0.70, 0.40)

# Texto
const GOLD_TEXT := Color(0.92, 0.78, 0.42)
const GOLD_TEXT_BRIGHT := Color(1.00, 0.88, 0.50)
const BONE_TEXT := Color(0.91, 0.86, 0.75)
const MUTED_TEXT := Color(0.55, 0.50, 0.42)
const DANGER_TEXT := Color(0.92, 0.42, 0.30)

# Barras (HP sangre, XP azul frio)
const HP_RED := Color(0.65, 0.10, 0.10)
const HP_RED_HI := Color(0.95, 0.30, 0.20)
const HP_RED_LOW := Color(0.42, 0.08, 0.06)
const XP_BLUE := Color(0.30, 0.55, 0.85)
const XP_BLUE_HI := Color(0.55, 0.80, 1.00)
const MANA_BLUE := Color(0.20, 0.40, 0.78)

# Categorías de skill
const SKILL_POISON := Color(0.40, 0.70, 0.30)
const SKILL_COLD := Color(0.40, 0.75, 0.95)
const SKILL_FIRE := Color(0.95, 0.50, 0.20)
const SKILL_LIGHTNING := Color(0.95, 0.92, 0.40)
const SKILL_PHYSICAL := Color(0.85, 0.78, 0.62)

# Acentos UI
const PANEL_BG := Color(0.06, 0.05, 0.04)
const SCREEN_OVERLAY := Color(0.0, 0.0, 0.0, 0.55)
const TUTORIAL_OVERLAY := Color(0.0, 0.0, 0.0, 0.78)